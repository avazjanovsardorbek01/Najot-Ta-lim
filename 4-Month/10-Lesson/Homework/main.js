// function getLastWordLength(str) {
//     // Stringni bo'sh joy bo'yicha ajratish
//     const words = str.trim().split(' ');

//     // Oxirgi so'zning uzunligini qaytarish
//     return words[words.length - 1].length;
//   }

//   // Misol
//   const sentence = "Bu yerda bir nechta so'zlar bor";
//   const lastWordLength = getLastWordLength(sentence);
//   console.log(lastWordLength); // Output: 3 (so'z "bor"ning uzunligi)

// function keyValuePairsToStringArray(obj) {
//     return Object.entries(obj).map(([key, value]) => key + value);
//   }

//   // Misol
//   const input = {a: 2, b: 5, c: 7};
//   const result = keyValuePairsToStringArray(input);
//   console.log(result); // Output: ['a2', 'b5', 'c7']
